# Supabase Configuration
# Get these values from your Supabase project dashboard
NEXT_PUBLIC_SUPABASE_URL=your_supabase_project_url
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_supabase_anon_key
SUPABASE_SERVICE_ROLE_KEY=your_supabase_service_role_key

# Instructions for Netlify Deployment:
# 1. Go to your Netlify site dashboard
# 2. Navigate to Site settings > Environment variables
# 3. Add the above environment variables with your actual Supabase values
# 4. Make sure to use the exact variable names as shown above
# 5. The SUPABASE_SERVICE_ROLE_KEY is only needed for server-side operations

# Note: Never commit your actual .env.local file to version control
# This .env.example file serves as a template for required environment variables